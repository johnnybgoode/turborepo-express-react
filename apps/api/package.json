{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "check-types": "tsc --noEmit",
    "clean": "rm -rf dist",
    "dev": "pnpm dotenv-run tsx --watch src/index.ts",
    "dotenv-run": "pnpx @dotenvx/dotenvx run -- ",
    "lint": "biome check --diagnostic-level=warn --write",
    "serve": "node dist/index.js",
    "test": "vitest run",
    "postinstall": "[ ! -e .env ] && cp -v .env.example .env || :"
  },
  "dependencies": {
    "body-parser": "^2.2.2",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@repo/biome-config": "workspace:*",
    "@repo/tsconfig": "workspace:*",
    "@repo/vitest-config": "workspace:*",
    "@types/body-parser": "^1.19.6",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/morgan": "^1.9.10",
    "@types/node": "^25.2.1",
    "@types/supertest": "^6.0.3",
    "@vitest/coverage-v8": "^4.0.18",
    "supertest": "^7.2.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
